glossario = {
    "variavel": "armazena um objeto, como se fosse um caixa",
    "list comprehension": "criacao de uma lista com argumentos em um unica linha",
    "wrap": "chamar uma funcao dentro de outra funcao",
    "loop": "repete um bloco de codigo quantas vezes for explicitado",
    "dicionarios": "armazenam informacoes a partir de chaves e valores",
}

definicao = glossario["variavel"]
print(f"Variavel:\n{definicao}")
definicao = glossario["list comprehension"]
print(f"\nList comprehension:\n{definicao}")
definicao = glossario["wrap"]
print(f"\nWrap:\n{definicao}")
definicao = glossario["loop"]
print(f"\nLoop:\n{definicao}")
definicao = glossario["dicionarios"]
print(f"\nDicionarios:\n{definicao}")
